/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package village;

/**
 *
 * @author Honza
 */
public class Player {

    private Map map;

    public Player(Map map) {
        this.map = map;
    }

/*    public boolean build(int x, int y) {
        if(map.getLayerConstructions().position[x][y] == null){
            System.out.println("Jsem tu a stavim! X, Y: "+x+", "+y);
            MapElement element = new bRoad(x, y, map.SIZE);
            element.loadImageFromFile();
            map.getLayerConstructions().addElement(element);
            //map.getLayerConstructions().position[x][y] = element;
            return true;
        }
        return false;
    }*/

}
